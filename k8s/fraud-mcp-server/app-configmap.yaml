# fraud-mcp-serverの最小スタブ(1ホップ先行検証用。本実装はPython/FastMCP、ADR 0007)。
# account-service-stub(k8s/account-service/app-configmap.yaml)と同一の最小起動時ロジック
# (loopback限定bind・接続元loopback再チェック・合言葉ヘッダー検証)。このホップの検証では
# fraud-mcp-server自身への着信(ingress)は使わないが、Pod構成の一貫性と将来のingress検証の
# ために同居させる。
apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-mcp-server-stub-app
  namespace: gekko
data:
  app.py: |
    import http.server
    import json
    import os
    import socketserver

    BIND_HOST = os.environ.get("APP_BIND_HOST", "127.0.0.1")
    APP_PORT = int(os.environ.get("APP_PORT", "9000"))
    HANDSHAKE_HEADER = os.environ.get("HANDSHAKE_HEADER_NAME", "x-gekko-handshake")
    HANDSHAKE_FILE = os.environ.get("HANDSHAKE_TOKEN_FILE", "/handshake/token")


    def is_loopback(addr):
        return addr in ("127.0.0.1", "::1")


    class Handler(http.server.BaseHTTPRequestHandler):
        def handle_one_request(self):
            if not is_loopback(self.client_address[0]):
                self.send_response(403)
                self.end_headers()
                self.close_connection = True
                return
            return super().handle_one_request()

        def do_request(self):
            try:
                with open(HANDSHAKE_FILE) as f:
                    expected = f.read().strip()
            except OSError:
                expected = None
            got = self.headers.get(HANDSHAKE_HEADER)
            if not expected or not got or got != expected:
                self.send_response(403)
                self.end_headers()
                self.wfile.write(b"handshake verification failed")
                return

            body = json.dumps(
                {
                    "method": self.command,
                    "path": self.path,
                    "sub": self.headers.get("x-auth-sub"),
                    "scope": self.headers.get("x-auth-scope"),
                    "jti": self.headers.get("x-auth-jti"),
                }
            ).encode()
            self.send_response(200)
            self.send_header("Content-Type", "application/json")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)

        do_GET = do_request
        do_POST = do_request
        do_PUT = do_request
        do_DELETE = do_request

        def log_message(self, fmt, *args):
            print("%s - %s" % (self.address_string(), fmt % args), flush=True)


    class Server(socketserver.ThreadingMixIn, http.server.HTTPServer):
        daemon_threads = True


    if __name__ == "__main__":
        srv = Server((BIND_HOST, APP_PORT), Handler)
        print(f"stub app listening on {BIND_HOST}:{APP_PORT}", flush=True)
        srv.serve_forever()
