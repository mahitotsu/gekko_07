# Keycloak用Envoyサイドカー設定(ADR 0016/0017)。account-serviceのingressパターン
# (ADR 0009/0012)と同じDownstreamTlsContextだが、jwt_authn/rbac/luaは持たない——ここでの
# 認可は業務認可(scope等)ではなく、許可された呼び出し元以外からのmTLS接続を通信路レベルで
# 拒否するだけの単純なroutrer転送。
#
# 8443(このリスナー)は以下4者専用のmTLS必須経路:
# - ext-authz-service/-cc(Token Exchange/client_credentials。ADR 0016)
# - account-service(jwt_authnのJWKS取得。ADR 0017でmTLS化)
# - edge-proxy(ブラウザ・kcadm.sh・verify-hop.shなどSPIFFE身元を持ちえない呼び出し元の
#   代理。ADR 0017)
# ADR 0017でedge-proxyを導入したことで、keycloakコンテナ自体は8080を持たなくなり
# (KC_HTTP_HOST=127.0.0.1。k8s/keycloak/deployment.yaml参照)、account-serviceと同じ
# 「単一filter_chainで完全にmTLS必須」構成に統合できた。
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-envoy
  namespace: gekko
data:
  envoy.yaml: |
    admin:
      address:
        socket_address: { address: 127.0.0.1, port_value: 9901 }
    node:
      id: keycloak-envoy
      cluster: keycloak
    static_resources:
      listeners:
        - name: ingress-mtls
          address:
            socket_address: { address: 0.0.0.0, port_value: 8443 }
          filter_chains:
            - transport_socket:
                name: envoy.transport_sockets.tls
                typed_config:
                  "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.DownstreamTlsContext
                  require_client_certificate: true
                  common_tls_context:
                    alpn_protocols: ["h2"]
                    tls_certificate_sds_secret_configs:
                      - name: "default"
                        sds_config:
                          resource_api_version: V3
                          api_config_source:
                            api_type: GRPC
                            transport_api_version: V3
                            grpc_services:
                              - envoy_grpc:
                                  cluster_name: spire_agent
                    combined_validation_context:
                      default_validation_context:
                        match_typed_subject_alt_names:
                          # ADR 0019: fraud-mcp-server自身が(旧ext-authz-serviceに代わって)
                          # Keycloakへ直接mTLS接続するようになった。
                          - san_type: URI
                            matcher:
                              exact: "spiffe://gekko.internal/ns/gekko/sa/default/fraud-mcp-server"
                          - san_type: URI
                            matcher:
                              exact: "spiffe://gekko.internal/ns/gekko/sa/default/ext-authz-service-cc"
                          - san_type: URI
                            matcher:
                              exact: "spiffe://gekko.internal/ns/gekko/sa/default/account-service"
                          - san_type: URI
                            matcher:
                              exact: "spiffe://gekko.internal/ns/gekko/sa/default/edge-proxy"
                      validation_context_sds_secret_config:
                        name: "ROOTCA"
                        sds_config:
                          resource_api_version: V3
                          api_config_source:
                            api_type: GRPC
                            transport_api_version: V3
                            grpc_services:
                              - envoy_grpc:
                                  cluster_name: spire_agent
              filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: ingress_http
                    route_config:
                      name: local_route
                      virtual_hosts:
                        - name: keycloak
                          domains: ["*"]
                          routes:
                            - match: { prefix: "/" }
                              route: { cluster: keycloak_app_upstream }
                    http_filters:
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
      clusters:
        # 同一Pod内のkeycloakコンテナ自身(8080)へ平文で転送する。mTLS終端はこのEnvoyで
        # 完結しており、Pod内loopback越えは同一ネットワーク名前空間のため素のHTTP/1.1でよい。
        - name: keycloak_app_upstream
          type: STATIC
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: keycloak_app_upstream
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address: { address: 127.0.0.1, port_value: 8080 }
        # SPIRE Agent Workload API(UDS)へのSDSチャネル(ADR 0012/0016)。
        - name: spire_agent
          connect_timeout: 0.25s
          http2_protocol_options: {}
          load_assignment:
            cluster_name: spire_agent
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        pipe: { path: /run/spire/sockets/agent.sock }
